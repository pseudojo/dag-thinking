{
  "mcpServers": {
    "headroom": {
      "type": "stdio",
      "command": "wsl",
      "args": [
        "-d", "Ubuntu",
        "--",
        "bash", "-c",
        "export PATH='/home/hgcho/.local/bin:$PATH' && uvx --python 3.13 --from 'headroom-ai[mcp,proxy]' headroom mcp serve"
      ]
    },
    "sequential-thinking": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]
    }
  }
}
